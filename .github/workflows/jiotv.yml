name: Generate JioTV Playlist

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Build JioTV Go
        run: |
          git clone https://github.com/jiotv-go/jiotv_go
          cd jiotv_go
          go build

      - name: Create Config File with Custom Headers
        run: |
          mkdir -p ~/.config/jiotv_go
          cat << 'EOF' > ~/.config/jiotv_go/config.toml
          ${{ secrets.JIO_CONFIG }}
          EOF

      - name: Generate EPG and Playlist
        run: |
          cd jiotv_go
          # Environment variables ya force flags naal run karna
          ./jiotv_go epg generate
